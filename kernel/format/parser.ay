use("core")
use("define")
use("block")
use("control-flow")

module(Atomy::Format):
  require(File expand-path("../../../lib/atomy/formatter.kpeg.rb", _FILE))

  class(Parser):
    class(<< self):
      parse(source) :=
        new(source) onto:
          unless(parse):
            raise-error

          result
