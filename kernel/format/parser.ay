use("core")
use("define")
use("block")
use("control-flow")

Atomy::Format open:
  require(File expand-path("../../../lib/atomy/formatter.kpeg.rb", _FILE))

  Parser open:
    singleton:
      parse(source) :=
        new(source) onto:
          unless(parse):
            raise-error

          result
