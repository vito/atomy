use("core")
use("define")

macro(_undefined):
  Undefined new(node line)

Atomy::AST open:
  Undefined = Node class:
    generate

    bytecode(g) := do:
      pos(g)
      g push-undef

    to-pattern := Atomy::Patterns::Any new
