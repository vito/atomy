use("core")
use("define")
use("node")

Atomy::AST open:
  ast(Undefined)

  Undefined bytecode(g) := do:
    pos(g)
    g push-undef

  Undefined to-pattern := Atomy::Patterns::Any new

macro(_undefined):
  Undefined new(node line)
