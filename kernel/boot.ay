macro (_LINE) line
macro (_FILE) Atomy::AST::File new(line)

macro (import(name)) `(require-atomy(~name))

"operators define data comparison control-flow
data-delta hashes patterns precision node block errors dynamic therie
mixins pretty concurrency io condition" split each (k):
  puts("loading " + k)
  import(File expand-path("../", _FILE) + "/" + k)
