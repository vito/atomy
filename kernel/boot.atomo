macro (__LINE__) line
macro (__FILE__) Atomo::AST::File new(line)

"operators define control-flow data comparison
block errors therie mixins pretty concurrency" split each { k |
  puts("loading " + k)
  load: ("kernel/" + k + ".atomo")
}
